# Optional: set this to enable LLM-written blog prose via the Anthropic API.
# Without it, vidblog falls back to a free rule-based writer (cleans up the
# transcript into readable paragraphs but won't be as polished).
ANTHROPIC_API_KEY=
